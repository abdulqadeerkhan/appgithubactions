name: "commands-workflow"

on:
  push:

jobs:
  my-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: Group logging
        run: |
          echo "::group::My group message"
          echo "msg1"
          echo "msg2"
          echo "::endgroup::"
